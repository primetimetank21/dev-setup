# Orchestration Log — Donald

**Date:** 2026-04-12T02:46:55Z  
**Agent:** Donald (Shell Dev)  
**Session:** install-script-fixes  

## Summary

Donald implemented fixes for GitHub issues #68 (stdout/stderr ordering) and #69 (CRLF guard). PR #70 (exec 2>&1 merge) and PR #71 (devcontainer.json onCreateCommand) were both created, reviewed by Mickey, approved, and merged to `develop` branch via squash + delete-branch + admin flag. Both PRs passed all CI checks (4/4).

## Activities

1. **PR #70 Submitted:** Issue #68 fix — `exec 2>&1` stdout/stderr merge
   - Branch: `squad/68-fix-output-ordering`
   - Changes: added `exec 2>&1` to `setup.sh` and `scripts/linux/setup.sh`
   - CI: 4/4 green
   - Rationale: merges stderr into stdout immediately after `set -euo pipefail`

2. **PR #71 Submitted:** Issue #69 fix — CRLF guard in devcontainer.json
   - Branch: `squad/69-devcontainer-crlf-guard`
   - Changes: added `onCreateCommand` to `.devcontainer/devcontainer.json`
   - Command: `find . -name '*.sh' | xargs sed -i 's/\r//'`
   - CI: 4/4 green
   - Rationale: strips CRLF files on Windows bind-mount before setup runs

3. **PR #70 Reviewed & Approved:** Mickey approved
   - Decision record: documented in PR comments and `.squad/decisions/inbox/donald-68-69-fixes.md`
   - Merge: `gh pr merge --squash --delete-branch --admin`

4. **PR #71 Reviewed & Approved:** Mickey approved
   - Merge: `gh pr merge --squash --delete-branch --admin`

## Outcomes

- Issue #68 fully implemented and merged
- Issue #69 fully implemented and merged
- Both fixes deployed to `develop` branch
- Decision record ready for Scribe to merge into decisions.md
- Branches deleted; develop branch advanced
