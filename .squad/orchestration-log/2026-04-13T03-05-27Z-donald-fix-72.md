# Orchestration Log: Donald — Fix for Issue #72

**Date:** 2026-04-13T03:05:27Z  
**Agent:** Donald  
**Event:** Rewrote scripts/linux/tools/copilot-cli.sh — PR #73 opened

## Summary

Donald rewrote the copilot-cli.sh install script with a more reliable idempotency pattern: directory existence check (`~/.local/share/gh/copilot`) instead of exit-code probing.

## Changes

- **Idempotency check:** Changed from `gh copilot --help` exit-code probe to filesystem check (`test -d ~/.local/share/gh/copilot && [ -n "$(ls -A ~/.local/share/gh/copilot 2>/dev/null)" ]`).
- **Install trigger:** Switched to `printf 'y\n' | timeout 60 gh copilot >/dev/null 2>&1` to pipe stdin and avoid TTY hangs.
- **Removed:** `gh extension install` path and `gh alias delete` path (not applicable for built-ins).
- **Auth check moved:** Placed before directory check to fail early on auth issues.

## Outcome

PR #73 opened on branch `squad/72-fix-copilot-binary-download`. CI passing. Decision documented to `.squad/decisions/inbox/donald-copilot-fix.md`. Ready for review.
