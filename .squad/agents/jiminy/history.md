# Jiminy's History

## Core Context

- **Project:** dev-setup - Replicable setup scripts for Dev Containers and Codespaces
- **Owner:** Earl Tankard, Jr., Ph.D.
- **Universe:** Disney Classic
- **Role:** Squad Hygiene Auditor (reviewer-gate)
- **Joined:** 2026-05-16
- **Charter:** `.squad/agents/jiminy/charter.md`
- **Model:** `claude-opus-4.6` (premium - reviewer-gate role)

## Day-1 context (handed to Jiminy at creation)

### Why Jiminy exists

Earl's team had recurring squad hygiene failures that he had to catch manually:

1. **Branch ancestry bleed (Sprint 7, 3+ occurrences):** Squad branches forked from other squad branches instead of `develop`, polluting PR diffs with unrelated commits.
2. **Squash merges (Sprint 2, Sprint 3):** Ralph violated the MERGE GATE by squashing PRs that should have been regular merges. Earl's standing directive: ALL merges use regular merge commits, never squash.
3. **Uncommitted histories (recurring):** Agents append learnings to `history.md` but Coordinator forgets to fire Scribe, so edits sit uncommitted on `develop`. Caught by Earl manually each time.
4. **Rogue file paths (2026-05-16):** Verifier batch agents wrote `VERIFICATION_REPORT.md` files at random paths (`.squad/agents/{name}/VERIFICATION_REPORT.md`, `.squad/verification-report.md`) instead of using the Source of Truth Hierarchy. Donald cleaned up via `.squad/orchestration-log/2026-05-16-verification-evidence.md`.
5. **Stale branches (post-sprint):** Remote `squad/*` branches not cleaned after merge until Earl asks. Ralph now owns end-of-session cleanup per directive (2026-05-16).

### Key files to know about

- `.squad/decisions.md` - team decision ledger (read for context on every spawn)
- `.github/agents/squad.agent.md` (or platform-equivalent) - authoritative governance
- Source of Truth Hierarchy (see charter Section: Scope) - defines legal write locations per file type
- `hooks/` directory in repo root - pre-commit + commit-msg + pre-push hooks (Conventional Commits + shellcheck)
- `CHANGELOG.md` - `[Unreleased]` section is the staging area for the next release
- `.gitattributes` - has `merge=union` driver for append-only `.squad/` files

### Standing user directives (Earl)

- **Caveman speak** preferred (short, direct, no big words)
- **Em dashes and non-ASCII chars FORBIDDEN** in PowerShell string literals (CP1252 byte 0x94 issue)
- **ALL merges use regular merge commits** - never squash, no exceptions
- **Branches forked strictly from `develop`**, never from other squad branches
- **End-of-session: ALWAYS delete stale branches** (local + remote) - Ralph owns this
- **Bad commit messages hard-reject** (commit-msg hook enforces)
- **Verifier batches must use Source of Truth Hierarchy paths only** - 3 legal locations: `history.md` (learnings), `decisions/inbox/` (decisions), `orchestration-log/` (batch evidence)

### Roster context at hire-time

Active squad (Disney Classic universe):

- Mickey - Lead (architecture, code review, triage)
- Donald - Shell Dev (bash/zsh, Linux)
- Goofy - Cross-Platform Dev (PowerShell, Windows)
- Pluto - Config Engineer (dotfiles, hooks, env)
- Chip - Tester (CI, idempotency, edge cases)
- Scribe - Session Logger (mechanical, silent)
- Ralph - Work Monitor (queue, backlog, end-of-session cleanup)
- Jiminy (new) - Squad Hygiene Auditor (process QA)

### Open issues at hire-time

19 issues filed 2026-05-16 from post-sprint audit: #221-#239. Priorities P0/P1/P2/P3, all labeled with `squad:{member}`. None marked `go:yes` yet - Earl will mark sprint-ready when ready to start next sprint.

P0 issues Jiminy should know about:

- **#221** - nvm.ps1 path resolution bug (Goofy)
- **#222** - Git tag hygiene: retroactively tag 0.1.0-0.7.0 + cut 0.8.0 (Mickey)
- **#239** - E2E install smoke test in CI across Linux/macOS/Windows (Chip)

### First task assigned at hire

None. Jiminy begins auto-running on the next Coordinator return-to-user.

## Learnings

- **2026-05-16 -- First audit run.** Tree clean, branches clean, labels good. Main issues: 10 non-merge commits directly on main (Sprint 5 era hotfixes), 2 rogue files (.squad/commit-msg-temp.txt, .squad/agents/chip/audit-findings.md), duplicate log dirs (.squad/log/ vs .squad/logs/). Scribe history.md missing ## Learnings section. Patterns noticed: hotfix sessions historically bypass develop-first rule and commit straight to main.
- **2026-05-16 -- First audit resolution (coordinator-applied).** All 4 auto-fixable items resolved: (1) `.squad/commit-msg-temp.txt` deleted; (2) `.squad/agents/chip/audit-findings.md` content merged into canonical `.squad/orchestration-log/2026-05-16T07-00-25Z-chip-testing-audit.md` (was a stub manifest, now has full findings appended), rogue deleted; (3) `.squad/logs/session_log.md` migrated to `.squad/log/session_log.md` and `.squad/logs/` dir deleted (canonical = singular `log/`); (4) Scribe history.md got `## Learnings` section. Main-branch direct commits ACCEPTED as historical -- Earl confirmed no retroactive history rewrite; standing directive (develop-first, merge commits only on main) enforces going forward.
- **2026-05-16 -- Post-retro-batch audit (auto-trigger #2).** Audited 3-spawn batch (Coordinator direct-push 0431dc8 + Chip PR #241 merged c02d679 + Goofy PR #242 merged 0fc8dcf). 4 lanes checked. 2 findings total: (1) SKILL.md at 7770 bytes vs expected 7900+ (minor -- content intact, likely byte-count estimate was off); (2) PR #242 missing `squad:goofy` label pattern -- actually present but named differently in label list vs expected `squad:*` glob (FALSE POSITIVE on re-check -- label `squad:goofy` IS present). Net result: 1 minor finding (SKILL.md size). All other lanes clean: working tree clean, no stale branches, merge commits correct, Co-authored-by trailers present on both feature commits (60b1de1, 4614e25), history.md updated by both agents, decisions inbox drained, Scribe not yet fired (expected).
- 2026-05-16 Hygiene retro complete -- 4 action items shipped (pre-spawn-checklist skill + squad-history-check CI gate + PR template + 6 standing rules). See .squad/log/2026-05-16-hygiene-retro-complete.md.
- **2026-05-16 -- Post-batch audit #3 (4-agent sprint: PRs #243/#244/#245/#246).** Audited Goofy (#221 nvm path fix), Mickey (#222 retroactive tags), Chip (#239 E2E install), Pluto (#240 pre-commit hygiene). 4 lanes x 4 PRs = 16 checks. Results: PR #243 CLEAN, PR #244 CLEAN, PR #245 2 issues (missing `type:` label; no tmux/psmux assertion on Windows per issue acceptance criteria), PR #246 2 issues (PR body does not use .github/pull_request_template.md structure -- missing ## Test plan / ## Related / ## Hygiene checklist sections; "Closes #240" present but not under ## Related heading). Cross-cutting: all branches correctly forked from develop, all commits have Co-authored-by trailer, all history.md entries are substantive, all .ps1/.yml/test files are ASCII-clean. Label gap: `area:windows` does not exist as a repo label (not used by these 4 PRs but was flagged by Coordinator as a recurring spawn-time gap). Recommended follow-ups: (1) PR #245 add missing `type:feat` or `type:feature` label; (2) PR #246 restructure body to match template or accept deviation; (3) consider creating `area:windows` label for future Windows-scoped work.
- **2026-05-16 -- Sprint 10 (formerly Sprint S) mid-sprint audit.** Detected 2 hygiene issues on develop (@ 7f350d8): (1) `.squad/agents/doc/history.md` modified with 37 new lines from Doc's Sprint 10 fact-check. Per sprint retro pattern, this is correctly deferred to Scribe's fold at retro-PR time -- NO ACTION. (2) `bradygaster-squad-sdk-0.9.4.tgz` untracked, 689 KB, 5/16/2026 19:50, zero references anywhere (verified). Leftover from squad upgrade workflow. ACTION TAKEN: Created PR #280 (squad/jiminy-gitignore-tgz) adding `*.tgz` to .gitignore + deleted rogue file. Reason: prevents recurrence; file is re-downloadable. Gap flagged: Coordinator manually invoked Jiminy on Earl's flag, but charter says "auto-trigger after multi-agent batches 3+". This batch had 6 squad agents + Doc (7 spawns) mid-sprint. Trigger was manual dispatch, not auto. Root cause: Coordinator dispatch-trigger is stated but not enforced. Recommendation: update charter section "Triggers" to clarify/automate OR add SOP to CONTRIBUTING.md "Coordinator MUST invoke Jiminy after every multi-agent batch 3+ before returning results to user". Action item for Sprint 10 retro: ensure Jiminy auto-trigger gap is resolved for future batches.
- **2026-05-18 -- Sprint 10 end-of-sprint audit (Coordinator auto-dispatch per PR #280 SOP).** Audited develop @ `6e8995a` after 9 merged PRs (#274-#283). Lanes checked: tree state, rogue files, decisions inbox, agent history.md coverage, branch/worktree hygiene, .gitignore coverage. Findings: (1) Working tree CLEAN. (2) No rogue files at repo root; no scratch `.md` at `.squad/` root. (3) `.gitignore` `*.tgz` rule from #280 present. (4) Decisions inbox NOT drained -- 2 entries from Doc waiting for Scribe: `doc-sprint-s-batch-fact-check.md` (21176 B) and `doc-pr-282-fact-check.md` (14499 B). Both gitignored, both already folded into `doc/history.md` via #281 + #283; Scribe can delete after confirming. (5) Agent history.md state: Donald has Sprint 10 entry in PR #278 (terse one-liner, but present); Pluto via #275 + #277; Goofy via #276 + #282; Chip via #279; Doc via #281 + #283; Jiminy mid-sprint entry via #280. GAPS: Mickey PR #274 (Sprint 9 (formerly Sprint R) retro action items) shipped WITHOUT appending to `mickey/history.md` -- last entry is still the 0.9.4 upgrade audit. Scribe MUST backfill a Mickey Sprint-10 entry in the retro PR. Scribe and Ralph have no Sprint-10 entries yet, which is expected (Scribe writes at retro time, Ralph writes at end-of-session cleanup time). (6) Branches: local clean (only `develop` + `main`), worktree clean (only primary). Six stale remote `squad/*` branches survive post-merge (`223-logging-consolidation`, `231-ps1-gitattributes`, `234-ps1-ascii-encoding`, `255-squad-cli-warning`, `255-tool-version-pins`, `271-uninstall-hookspath`) -- Ralph end-of-session cleanup. AUTO-FIXES APPLIED: none (no rogues to move/delete; inbox drain routes to Scribe per charter; my own history entry handed to Scribe per "if your only delta is history.md, hand to Scribe" rule -- did NOT create a self-branch). Trigger: Coordinator dispatched per the SOP added in #280; auto-trigger gap from mid-sprint audit is now closed.
- **2026-05-19 -- Sprint 11 (formerly Sprint T) Wave 1 post-batch audit (first formal post-batch under #293 SOP).** Audited develop @ `b5992d9` after 3 merged PRs (#296, #297, #298) from Mickey (#229), Goofy (#230), Pluto (#233). This is the first >= 3-agent batch since the 3-surface SOP was codified in PR #293. Full 7-lane checklist: (1) History files: Mickey has Sprint 11 #229 entry (line 1005+), Goofy has Sprint 11 #230 entry (line 496+), Pluto has Sprint 11 #233 entry (line 428+) -- all present. Scribe and Ralph most-recent entries reference post-0.9.0 retro + EOS (PRs #294 + #295) -- correct. (2) Decisions inbox: EMPTY -- clean. (3) Rogue files: none under `.squad/` root beyond standard tracked files (.first-run, ceremonies.md, config.json, decisions.md, routing.md, team.md). (4) Log dir: `.squad/log/` exists (singular, correct); `.squad/logs/` does NOT exist (deprecated dir absent -- good). (5) Stale branches: zero local `squad/*` branches, zero remote `squad/*` branches -- Coordinator already cleaned. (6) CHANGELOG `[Unreleased]`: all 3 issues represented -- #233 (2 entries under Changed), #229 (1 entry under Changed), #230 (1 entry under Changed). No duplication. (7) 3-surface SOP intact: Jiminy charter Triggers table references post-batch + 3-surface note (line 57+61); Doc charter documents dedicated worktree pattern (line 102+); `loop.md` Gate 1 references post-batch audit (line 58+); `ceremonies.md` Sprint Wrap references Jiminy session-end audit (line 121+). ALL LANES CLEAN. Auto-fixes: none needed. Only delta is this history.md entry. Pattern note: `gh pr merge --delete-branch` ghost branches recurred (3-for-3 on recent merges per Ralph's #295 entry) -- flagging for Coordinator as candidate tracking issue.
- **2026-05-19 -- Sprint 11 session-end audit (Sprint Wrap ceremony step 1).** Audited develop @ `033f80e` after all 4 Sprint 11 issues shipped (#229 PR #298, #230 PR #297, #233 PR #296, #292 PR #301) plus bonus PR #299 (Wave 1 audit) and issue #300 (gh quirk tracker). Full 11-lane sweep: (1) Agent history entries: Mickey has #229 entry (line 1005+), Goofy has BOTH #230 (line 496+) and #292 (line 550+), Pluto has #233 (line 428+), Jiminy Wave 1 entry present (this file, line 80). ALL PRESENT. (2) Decisions inbox: EMPTY. (3) Rogue files: none at repo root or `.squad/` root. (4) Log dir: `.squad/log/` (singular) exists; deprecated `.squad/logs/` absent. (5) Stale branches: zero local, zero remote `squad/*` after `git fetch --prune`. (6) CHANGELOG `[Unreleased]`: PR #296 under Changed (PSSA docs), PR #297 under Changed (auth.ps1 move), PR #298 under Changed (ARCHITECTURE refresh), PR #301 under Fixed ($LASTEXITCODE) -- all present, no duplication; PR #299 correctly has no entry (hygiene-only). (7) SOP integrity: Jiminy charter intact (line 1+), Doc charter worktree pattern (line 102+), `loop.md` Gate 1 (line 58+), `ceremonies.md` Sprint Wrap (line 121+). (8) SKILL.md audit table: all 5 sites marked **Mitigated** in PR #292 (lines 156-160). (9) Group EE tests present in `tests/test_windows_setup.ps1` (line 1886+). (10) Issue #300 open with correct labels (`type:chore`, `priority:p3`, `squad:jiminy`). (11) Retro candidates: (a) Wave 1 post-batch gate (PR #299) fired cleanly -- no friction, 0 fixes needed; (b) Doc worktree SOP untested this sprint (Doc did not run -- mechanical sprint, non-issue); (c) Sequential Goofy (#230 then #292) worked as planned, no churn; (d) `gh --delete-branch` quirk at 75% rate already tracked in #300. ALL LANES CLEAN. Verdict: `Jiminy clear`.
- **2026-05-17 -- Sprint 12 Wave 1 audit: #300 gh-merge-quirk close decision.** Audited develop @ `66930c6` (worktree `dev-setup-300` on branch `squad/300-gh-quirk-close`). Scope: re-evaluate the `gh pr merge --delete-branch` ghost-remote-branch quirk tracked in #300 (historical fail rate 5-of-6 at filing). Method: (1) `git fetch origin --prune` then `git branch -r` -- result: only `origin/develop` + `origin/main`, zero stale `squad/*` remotes. (2) `gh pr list --state merged --limit 30` -- pulled last 30 merges (25 with `squad/*` heads). (3) Cross-referenced merged PR head refs against live remote refs -- zero `squad/*`/`chore/*`/`release/*` heads survive post-merge. Specifically post-#300-filing: PRs #299, #301, #302, #303, #311, #312 all clean (6-for-6). The two most recent (#311 + #312) used the exact `--admin --squash --delete-branch` flag combination that previously triggered the quirk -- both deleted cleanly. **Decision: CLOSE #300 as no-longer-reproducible (Option A per dispatch).** Likely upstream `gh` CLI fix or trigger condition has shifted. Filed close comment on issue + decision drop at `.squad/decisions/inbox/jiminy-gh-quirk-close-20260517.md`. No PR opened (no code change needed). The post-merge `git push origin --delete <branch>` fallback from PR #295 remains in Ralph's EOS toolkit if recurrence happens. Worktree `dev-setup-300` + branch `squad/300-gh-quirk-close` switched back to develop and handed to coordinator for removal.
- **2026-05-17 -- Sprint 12 Wave 1 post-batch audit (mandatory gate per loop.md / ceremonies.md).** Audited develop @ `be251b4` after 4 merged PRs (Mickey #314 closes #309, Donald #313 closes #236, Pluto #315 closes #254, Chip #316 closes #238) plus the parallel #300 close (no PR). Full 11-lane sweep. Findings: (1) Rogue files: ONE known loose file `.squad/agents/jiminy/history.md` (my prior #300 audit edit on develop -- the well-known violation that triggered this gate). No OTHER rogues anywhere. (2) Decisions inbox: exactly 2 files as advertised (`jiminy-2026-05-17-post-batch-audit-fold.md` 4838 B pre-existing; `jiminy-gh-quirk-close-20260517.md` 1380 B fresh) -- count + names match expectation, Scribe owns drain. (3) Branches: `git branch` shows develop + main only; `git branch -r` shows origin/develop + origin/main + HEAD only; `git worktree list` shows only the primary at `C:\Users\Earl Tankard\Coding\dev-setup`. Zero stale `squad/*` survivors. (4) Tags: main @ `724c62c` still on `0.9.1` (untouched, no accidental push); develop @ `be251b4`; tag list head unchanged (0.9.1, 0.9.0, 0.8.0, 0.1.0, 0.2.0). (5) CHANGELOG `[Unreleased]`: all 4 entries present and ASCII-clean -- Added: Group FF (#238 / #316); Changed: ARCHITECTURE.md Script Conventions (#309 / #314), sprint-naming revert, .aliases bash/zsh-only header (#236 / #313); Removed: legacy priority labels (#254 / #315). No duplication. (6) History.md coverage: Mickey has #314 ARCHITECTURE entry (verified -- tail mentions Sourcing/Read-ToolVersion + ASCII-clean diff note); Donald has #313 .aliases entry (verified -- "Sprint 12 -- PR #313: Issue #236" header); Pluto has #315 label-hygiene entry (verified -- "PR: #315 (filed end-of-session)" + label-hygiene SOP capture); Chip has #316 Group FF entry (verified -- FF-7 through FF-10 + sandbox gotcha block); Jiminy has #300-close entry (line 82, present in the loose history.md); Scribe + Ralph correctly absent (Scribe writes at fold time, Ralph at EOS). (7) Log dir: `.squad/log/` exists (singular); `.squad/logs/` confirmed absent. (8) Issue labels: #300 CLOSED with `priority:p3`, `go:yes`, `release:backlog`, `type:chore`, `squad:jiminy` -- complete; #317 OPEN with `priority:p3`, `type:chore`, `squad:jiminy`, `area:meta` -- MINOR FLAG: missing `release:backlog` (every other untargeted issue carries it). Not auto-fixing (label edits are Mickey's territory per charter Boundaries). (9) Open PRs: `gh pr list --state open` returns `[]` -- zero. (10) `.gitattributes` `merge=union` rules: all 4 present and correct (`.squad/decisions.md`, `.squad/agents/*/history.md`, `.squad/log/**`, `.squad/orchestration-log/**`); `hooks/pre-commit` allow-lists `.squad/skills/*/SKILL.md` -- verified, Pluto's #315 SKILL.md exists and merged cleanly. (11) Spawn hygiene compliance: PR #313 touched (donald history + CHANGELOG + README + .aliases) -- compliant; PR #314 touched (mickey history + CHANGELOG + ARCHITECTURE) -- compliant; PR #315 touched (pluto history + CHANGELOG + label-hygiene SKILL) -- compliant + optional skill; PR #316 touched (chip history + CHANGELOG + test_windows_setup.ps1) -- compliant. All 4 PRs followed the hygiene-tail spec. Auto-fixes applied: NONE -- the known loose history.md routes to Scribe per the "history-only delta hands to Scribe" rule, inbox drain is Scribe's, label gap on #317 is Mickey's. No fix PR opened (no rogues to clean, no scope creep). Verdict: `Jiminy fixed 0 items, flags 1 minor (label gap on #317)`. Trigger: auto-dispatch per the post-batch gate codified in PR #293 (loop.md Gate 1 + ceremonies.md Sprint Wrap + Jiminy charter Triggers).



- **2026-05-17 -- Sprint 12 Wave 2 post-batch audit (mandatory gate per loop.md / ceremonies.md).** Audited develop @ `69391b5` after 3-agent batch: Mickey #310 / PR #321 (open, MERGEABLE; ARCHITECTURE.md Windows Dep Order chain), Donald #237 / PR #320 (open, MERGEABLE; CONTRIBUTING.md test harness pattern + new skill `.squad/skills/test-harness-pattern/SKILL.md`), Goofy #235 (closed NOT_PLANNED via Case B -- no PR, no branch pushed). Full 11-lane sweep. Findings: (1) **CRITICAL - Worktree-isolation violation by Mickey.** Worktree `dev-setup-310`'s `.squad/decisions/inbox/` is EMPTY; Mickey's drop `mickey-arch-windows-dep-20260517.md` (3432 B) landed in the MAIN checkout's inbox instead. Combined with the earlier stray ARCH em-dash edit on main (reverted by coordinator), this is the SECOND distinct write-to-wrong-CWD event in the same agent run -- a real worktree-discipline gap, not a one-off. Root cause hypothesis: dispatch prompt did not pin CWD to the worktree before file writes; agent inferred path from `TEAM_ROOT` but tooling silently resolved against the process CWD (main checkout). Donald's parallel spawn for #237 wrote drop correctly into `dev-setup-237` inbox -- so the failure mode is non-deterministic and likely environment-state-dependent. Remediation drop filed at `.squad/decisions/inbox/jiminy-wave-2-audit-20260517.md`. (2) **MEDIUM - Pre-commit hook ASCII-scan scope gap.** `hooks/pre-commit` Check 2 (`ASCII-only on staged`) globs ONLY `*.ps1` files (see `grep '\.ps1$'` at line 35). All `*.md` and other source files are unguarded. Confirmed: HEAD's `ARCHITECTURE.md` carries 134 non-ASCII line hits (em-dashes U+2014, box-drawing U+2500/U+2502/U+251C/U+2514, smart-arrow U+2192) and they have been there since at least PR #314 (Mickey's own #309 entry notes `ARCHITECTURE.md has pre-existing non-ASCII bytes elsewhere; the pre-commit hook only guards *.ps1`). Sweep-wide: 134 lines in ARCH, 60 in README, 12 in CONTRIBUTING, plus dozens of `.squad/` and `.copilot/` files (counts logged in audit). Recommend separate follow-up ticket: either (a) extend Check 2 to `.md` (with a one-time normalize sweep first) or (b) explicitly scope ASCII to `*.ps1` + `*.psm1` + `*.psd1` in the hook comment and leave `.md` to author discretion. Not auto-fixing -- scope creep. (3) **OK - develop branch staging.** `M .squad/agents/goofy/history.md` is UNSTAGED on main checkout (not staged as the brief said -- minor discrepancy). Per the standing develop-first rule, will be folded by Scribe in the next consolidation PR (alongside this Jiminy audit entry and the 3 harvested inbox drops). NO direct develop commit needed. Coordinator's planned flow handles it correctly. (4) **OK - PR labels.** Both PRs were spawned with EMPTY label sets. Auto-fixed: PR #321 labelled `squad:mickey, priority:p3, release:backlog, type:docs, area:meta` (mirrors #310 minus `go:yes`); PR #320 labelled `squad:donald, priority:p3, release:backlog, type:docs` (mirrors #237 minus `go:yes`). (5) **OK - Issue #235 closed-not-planned.** Lingering `go:yes` label removed (closed-issue hygiene). Remaining labels: `squad:goofy, priority:p3, release:backlog, type:chore`. (6) **OK - History tails.** Mickey appended Wave 2 entry in worktree -310 (will fold on PR #321 merge); Donald appended in worktree -237 (will fold on PR #320 merge); Goofy's harvested entry is on develop (51 lines, unstaged, Scribe-pending). (7) **OK - Skill template compliance.** `.squad/skills/test-harness-pattern/SKILL.md` (Donald) has full frontmatter (`name`, `description`, `domain: testing`, `confidence: medium`, `source: observed`) + all 4 required sections (Context, Patterns, Examples, Anti-Patterns) per `.squad/templates/skill.md`. (8) **OK - Inbox drops well-formed.** All 3 main-checkout drops (`mickey-arch-windows-dep`, `donald-test-harness`, `goofy-install-guard-deferral`) are ASCII-clean, have `### date: title` header + `**By:**` + `**What:**` + `**Why:**` fields; gitignore confirmed (`.gitignore:4:.squad/decisions/inbox/`). (9) **OK - Branches/worktrees.** Main checkout on `develop` @ `69391b5`; worktree -310 on `squad/310-arch-windows-dep-order` (clean, pushed to origin); worktree -237 on `squad/237-test-harness-pattern` (clean, pushed). No stray worktrees, no stale local branches. (10) **OK - Repo root.** No rogue files outside the expected set (ARCH, CHANGELOG, CONTRIBUTING, README, setup.sh, setup.ps1, .tool-versions, config dirs). Tests/ + scripts/ uninspected for additions in this batch (no agent wrote there). (11) **OK - .gitattributes `merge=union`.** Rules intact for `decisions.md`, `agents/*/history.md`, `log/**`, `orchestration-log/**` -- Scribe fold will merge cleanly. **Auto-fixes applied:** (a) PR #321 + PR #320 full label sets; (b) `go:yes` removed from closed #235. **Flags for Coordinator:** (i) Mickey worktree-isolation violation -- adopt pre-spawn `Push-Location ` wrapper OR explicit CWD-verify step in agent prompts (see remediation drop); (ii) pre-commit ASCII-scan `.md` gap -- file follow-up issue, separate sweep. **Verdict:** `Jiminy fixed 3 items (2 label sets + 1 stale label), flags 2 (worktree-isolation pattern + hook scope gap).` Trigger: auto-dispatch per post-batch gate (3-agent threshold met).
- **2026-05-17 -- Sprint 12 session-end audit (Sprint Wrap, develop `5dfc476`).** All 9 Sprint 12 issues closed across 3 waves + 2 fold PRs (10 PRs total: #313/#314/#315/#316/#318/#320/#321/#323/#324). REPO STATE: tree clean, only main worktree, 0 local orphan squad/* branches, 4 STALE local remote refs (`origin/squad/{237,306,310,scribe-sprint-12-wave-2-fold}`) -- `git remote prune origin` would clean (Ralph EOS scope). Remote has only `develop` + `main` (gh API confirmed). LABEL STATE: 8 squad:* labels present (chip/doc/donald/goofy/jiminy/mickey/pluto/ralph) -- `squad:scribe` MISSING (gap surfaced by issue #319). PR STATE: 0 open, recent merges -- only PR #320 (Donald docs) and PR #321 (Mickey docs) carry full label set among Sprint 12 merges; PRs #313/#314/#315/#316/#318/#323/#324 have NO labels at all (process gap -- PR labels not enforced on merge). ISSUE STATE: 0 open `go:yes`, 5 follow-ups all open: #326/#325/#322 (full 5-label set), #317 (full 5-label set incl squad:jiminy), #319 (4 labels, MISSING squad:* because `squad:scribe` doesn't exist). HYGIENE-TAIL: Mickey 3/3 entries (PRs #314/#321/#324), Donald 2/2 (#313/#320), Chip 1/1 (#316 Group FF), Pluto 1/1 (#254/#315), Goofy 1/1 (#235 Case B deferral), Jiminy 3/3 prior (#300 close + Wave 1 + Wave 2 audits) -- this entry makes 4, Scribe 2/2 (Wave 1 fold #318, Wave 2 fold #323) -- ALL AGENTS COMPLIANT. SKILL FILES: `.squad/skills/label-hygiene/SKILL.md` (Pluto, full frontmatter: name/description/domain/confidence:medium/source:earned-#254) and `.squad/skills/test-harness-pattern/SKILL.md` (Donald, full frontmatter: name/description/domain:testing/confidence:medium/source:observed) -- both template-compliant. `abstraction-threshold` (Goofy Wave 2 deferral) NOT formalized as expected -- skills dir has 12 entries, no `abstraction-threshold/`. INBOX HYGIENE FLAG: `.squad/decisions/inbox/` still has 4 files from Wave 2 (donald-test-harness, goofy-install-guard-deferral, jiminy-wave-2-audit, mickey-arch-windows-dep). decisions.md grew 44473 -> 57253 bytes (delta 12780 ~= sum of 4 inbox files 12700 bytes), so Scribe DID drain content but did NOT delete the drop files. Scribe-domain bug, NOT auto-fixed per audit scope (Scribe owns drain). CHANGELOG `[Unreleased]`: 9 entries (2 Added + 6 Changed + 1 Removed) closing 7 issues (#306/#309/#310/#236/#237/#238/#254) + 2 internal (Sprint naming Tier 3 sweep, Wave 2 fold note). NON-TRIVIAL -- 0.9.2 cut justified.
- **Auto-fixes applied this audit:** ZERO label modifications applied -- `squad:scribe` does not exist as a GitHub label, so adding to issue #319 requires either (a) Pluto creates the label first or (b) Coordinator decides to leave un-squadded. Surfaced for Coordinator. NO file modifications, NO branch operations, NO PR/issue state changes -- read-mostly audit completed cleanly.
- **Lesson (Scribe inbox-drain bug):** Scribe Wave 2 fold (PR #323) merged inbox content into `decisions.md` but did NOT delete the source drop files. Either the fold script's drain step ran after staging but failed silently, OR the fold author treated "drain" as "merge content" without "delete file". Recommend Scribe retro topic: "after `git add -- decisions.md`, also `git rm -- decisions/inbox/*.md` in the SAME commit so drain is atomic with merge."
- **Lesson (squad: label set is incomplete):** Standard label set covers 8 active engineering agents but omits `squad:scribe`. When a follow-up issue is rooted in a service-role agent's domain (Scribe, future similar), the label vocabulary forces a routing miss. Recommend Pluto's next label sweep create `squad:scribe` (and audit whether `squad:ralph` is being used at all -- precedent exists for service-role labels).