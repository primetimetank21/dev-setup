# Session Log — CRLF Dotfiles Fix

**Date:** 2026-04-12
**Requested by:** primetimetank21
**Squad:** Mickey (Lead), Donald (Dev), Scribe

## Summary

Fixed CRLF line ending issues in shell dotfiles that caused `^M` errors when sourced in the Linux devcontainer.

## Work Done

- Mickey created Issue #87: "Fix: CRLF line endings in dotfiles cause ^M errors in Linux shell"
- Donald implemented fix on branch `fix/crlf-dotfiles`:
  - Added `text eol=lf` rules to `.gitattributes` for `.aliases`, `.vimrc`, `.zshrc.template`, `.npmrc.template`, `.gitconfig.template`, `.editorconfig`, `config/dotfiles/install.sh`, `setup.sh`
  - Marked `setup.ps1` as `-text` in `.gitattributes`
  - Expanded `onCreateCommand` in `.devcontainer/devcontainer.json` to strip `\r` from `.aliases`, `.vimrc`, `*.template`
  - Ran `git add --renormalize .` to normalize existing files
  - Opened PR #89 to `develop`
- Mickey reviewed and approved PR #89
- Scribe merged PR #89 (squash, branch deleted) after CI green

## Files Changed

- `.gitattributes` — added dotfile LF rules, preserved `.squad/` merge=union entries
- `.devcontainer/devcontainer.json` — expanded onCreateCommand

## Issues Closed

- Fixes #87
